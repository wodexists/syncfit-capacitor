rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Temporarily allow all reads and writes for development and testing
      // IMPORTANT: Change this to more restrictive rules before production deployment
      allow read, write: if true;
    }
  }
}